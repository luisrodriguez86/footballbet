{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Games</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Localgoals</th>
                <td>{{ entity.localGoals }}</td>
            </tr>
            <tr>
                <th>Awaygoals</th>
                <td>{{ entity.awayGoals }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('games') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('games_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
